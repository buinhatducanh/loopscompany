generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String?
  name      String
  role      String   @default("user")
  googleId  String?  @unique
  avatar    String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model SiteSetting {
  id        String   @id @default("default")
  config    Json
  updatedAt DateTime @updatedAt
}

model SeoPage {
  id            String   @id @default(cuid())
  pageId        String   @unique
  label         String
  title         String
  description   String   @db.Text
  keywords      String?
  ogTitle       String?
  ogDescription String?  @db.Text
  canonical     String
  ogImage       String?
  robots        String   @default("index, follow")
  updatedAt     DateTime @updatedAt
}

model Article {
  id            String   @id @default(cuid())
  slug          String   @unique
  category      String
  categoryColor String   @default("#16a34a")
  title         String
  excerpt       String   @db.Text
  content       String   @db.Text
  cover         String
  coverPublicId String?
  author        String
  authorRole    String
  publishedAt   DateTime
  readTime      Int      @default(5)
  tags          String[]
  published     Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model ContactLead {
  id        String   @id @default(cuid())
  name      String
  email     String
  phone     String?
  message   String   @db.Text
  source    String   @default("website")
  createdAt DateTime @default(now())
}

model TeamMember {
  id          String   @id @default(cuid())
  name        String
  role        String
  avatar      String
  years       Int
  badge       String
  bio         String   @db.Text
  skills      String[]
  isSpotlight Boolean  @default(false)
  specialty   String?
  order       Int      @default(0)
  active      Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Project {
  id        String   @id @default(cuid())
  num       String   // Ví dụ "01"
  title     String   // Ví dụ "VinFast Toàn Cầu"
  category  String   // Ví dụ "WEB · STRATEGY"
  year      String   // Ví dụ "2024"
  result    String   // Ví dụ "+340% traffic"
  img       String   // URL hình ảnh
  accent    String   // Màu nhấn HEX "#C8A261"
  url       String?  // URL liên kết (tuỳ chọn)
  createdAt DateTime @default(now())
}

